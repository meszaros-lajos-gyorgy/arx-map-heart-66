import { Texture } from 'arx-level-generator'

export const doorstepTop = Texture.fromCustomFile({
  filename: '[wood]doorstep-top.jpg',
  sourcePath: 'textures',
})

export const doorstepSide = Texture.fromCustomFile({
  filename: '[wood]doorstep-side.jpg',
  sourcePath: 'textures',
})
